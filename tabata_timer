tabata-timer/
├── index.html
├── script.js
└── style.css   (optional)

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tabata Timer</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      text-align: center;
      padding: 40px;
    }
    input, button {
      margin: 10px;
      padding: 8px;
      font-size: 16px;
    }
    h1, h2, h3, h4 {
      margin: 20px 0 10px;
    }
  </style>
</head>
<body>
  <h1>Tabata Timer</h1>
  <div>
    <label>Active Time (seconds): <input type="number" id="activeTime" value="20"></label><br>
    <label>Rest Time (seconds): <input type="number" id="restTime" value="10"></label><br>
    <label>Rounds: <input type="number" id="rounds" value="8"></label><br>
    <button id="startButton">Start</button>
  </div>
  <h2 id="status">Ready</h2>
  <h3 id="countdown">0</h3>
  <h4 id="roundDisplay">Round: 0</h4>

  <script>
    const startButton = document.getElementById('startButton');
    const countdown = document.getElementById('countdown');
    const statusDisplay = document.getElementById('status');
    const roundDisplay = document.getElementById('roundDisplay');

    let activeTime, restTime, rounds;
    let currentRound = 0;
    let isActive

